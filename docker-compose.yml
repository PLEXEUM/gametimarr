services:
  gametimarr:
    build: .
    container_name: gametimarr
    ports:
      - "7667:7667"
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
      - /path/to/your/media:/media/sports
    environment:
      - TZ=America/New_York
      - LOG_LEVEL=INFO
    restart: unless-stopped