# This file exists so Git tracks the empty data/ directory.
# The SQLite database (gametimarr.db) will be created here at runtime.
# Contents of this directory are excluded from version control via .gitignore.